Original prompt: Review the code and launcht the website and we will start making updates

- April 3, 2026 desktop hover cleanup: restricted the floating focused-card preview over the enemy half to the mobile layout only. Wide desktop now keeps the existing hand hover/tooltip behavior without the oversized mobile-style preview covering the enemy board, and window resize now re-syncs the floating preview plus the lifted hand card layer immediately.
- Added and iterated on turn flow, placement, energy, and battle rules across the Spring Boot web game.
- Latest change: moved board notch dots to the actual card boundary so edge/corner links render on the rectangle perimeter instead of floating inward.
- April 1, 2026 popup preview follow-up: restored the floating card preview as a full rendered card instead of the old text summary box. Locked hand cards can now still be selected for inspection, the selected-card drawer reuses the same full-card renderer, and both views show notch dots on the real card perimeter. Local Playwright screenshots in `output/web-game/popup-restored-check` confirmed the popup and drawer on both mobile and desktop after restarting Spring Boot from source.
- Added Earth and Wind as first-class elements using the Roblox reference project's names/colors/archetypes (`CreatureData.lua` / `ArenaSystem.lua`), expanded energy tracking/UI to four elements, and switched the starter matchup to Fire/Earth vs Water/Wind.
- Raised Siegling HP floors so all creature cards now start at 10+ HP via the scaled health helper in `src/main/java/com/sieglings/service/CardDefinitionService.java`.
- Reworked internal mixed-element links into combo points. Same-element links are now the only source of normal internal energy; mixed internal points produce combo records like `EARTH+FIRE`, including 3-way and 4-way crossroads.
- Added combo-spell requirements in `SpellCard` plus starter-deck combo spells such as `Emberroot Burst` and `Storm Current`. Live API now exposes combo counts and combo point signatures to the UI.
- Replaced sidebar energy stats with a visual Energy Bucket made of per-energy circles plus combo pie tokens, and added an element color key to the right panel.
- Added a pre-match loadout flow with 8 preset decks (4 single-element, 4 mixed including one four-element deck) and SiegeKnight selection before the game starts. Backend exposes `/api/game/options`, and `new game` now accepts `deckId` + `trainerId`.
- Added trap cards as a full card type. Traps now serialize into the hand/UI, use opponent energy-bucket requirements instead of your own energy, and the AI can spring them during setup. Shared action-card targeting now reads `targetType` metadata instead of guessing from description text.
- Added a one-Siegling-per-turn placement cap for both player and AI. Game state now tracks per-turn placements, the backend blocks second placements with a clear log message, and the frontend stops highlighting placement cells after your first placement resolves.
- Added stronger trap support in deck construction by giving trap cards a light playset presence instead of singleton odds, so elemental and mixed decks draw them more consistently.
- Added elemental weakness bonus damage for Siegling attacks using the Roblox reference chart subset now active in the TCG. Current live matchups: Water gets +1 vs Fire/Earth, Earth gets +1 vs Wind, and Wind gets +1 vs Fire. Weakness bonus is applied only to source-based Siegling damage, not generic spells/traps.
- Adjusted board card layout so creature names sit centered inside the cell while HP/stats/statuses are anchored above the bottom edge, reducing overlap with lower notch points and the stat strip.
- Added selected common battle fallbacks: a few commons now get zero-cost weak poke attacks or zero-cost self-move abilities that reposition them to an open linked point during battle. Verified live that `Flame Sprite` exposes `Flame Sprite Drift` as a 0-energy `SELF` action.
- Added Roblox-backed evolution placement rules for the current TCG lines: `Cacty -> Jacked'ty`, `Pylme -> Bonoblade`, `Breezee -> Gagglestand`, and `Pursula -> Purseus`. Evolution cards now target the occupied precursor cell, and the evolved board instance merges inherited notches with the new card's added links. Live API check confirmed `Player evolves Cacty into Jacked'ty...` and the merged notch set `TOP,RIGHT,BOTTOM,LEFT`.
- Added Shadow and Electric as live TCG elements while keeping Water active. The backend now tracks six elemental energies, the deck builder/startup picker includes `deck_shadow` and `deck_electric`, and the generated catalogs now include Roblox-backed Shadow and Lightning/Electric rosters plus 30 spells / 2 traps per new element.
- Added `Night Regent` and `Volt Shepherd` as SiegeKnight options, updated the UI color system / energy bucket / element key for Shadow and Electric, and fixed `GameService`'s preset fallback so the app no longer depends on a hardcoded deck list index.
- Latest verification: `-DskipTests compile` passes, the app is running again on port 8080 from `spring-boot-16.log`, `/api/game/options` now returns `10` decks and `8` trainers, and a live `deck_shadow` start produced a Shadow opening hand with `shadowEnergy` and `electricEnergy` fields present in the serialized player state.
- Node/npx are not installed in this environment, so the Playwright loop from the web-game skill could not be run locally. Verification is currently manual via the running Spring Boot app at http://localhost:8080.
- Latest manual verification: `-DskipTests compile` passes, the app is running again on port 8080, mono Fire now starts from a 17-card list (`deckSizeAfterDraw: 12` after opening hand), and a live battle log confirmed weakness damage: `Coral Seahorse Strike deals 3 damage to Apple Head (weakness +1)`.
- If further visual tuning is needed, continue adjusting `.board-notches` inset and the `.notch-*` anchor positions in `src/main/resources/static/css/style.css`.
- Added a dedicated compact mobile layout for gameplay. The left panel now renders as concise enemy/player/action summary blocks, the right panel now has mobile-only tabs for `Battle / Card / Log / Key`, the board compresses to a smaller responsive footprint, and the hand switches to a dense 3-column card grid with truncated copy instead of a long scrolling strip.
- Updated markup in `src/main/resources/templates/game.html`, mobile interaction state in `src/main/resources/static/js/game.js`, and responsive styling in `src/main/resources/static/css/style.css`.
- Latest verification: `-DskipTests compile` passes, the Spring Boot app was restarted on port 8080 from `spring-boot-19.log`, and both the local and tunneled HTML now serve the new `summary-block` + `mobile-info-tabs` markup. Playwright visual validation is still blocked because `node`/`npx` are not installed in this environment.
- Firebase/mobile startup bug fix: the hosted app was rebuilding the full deck + SiegeKnight chooser every 2 seconds while an online room was waiting because `startRoomPolling()` called `renderLoadoutOptions()` on every poll. That now only refreshes the room status + summary, which stops the chooser from looking like it is constantly reloading.
- Firebase/mobile startup bug fix: removed the default `visible` class from the loadout overlay in `src/main/resources/static/index.html` so the hosted build no longer flashes the chooser before the app decides whether it actually needs to open it.
- Latest verification: Firebase Hosting redeployed successfully to `https://siegelingstcgtesting.web.app`, the hosted HTML now serves `<div class="loadout-overlay" id="loadoutOverlay">` without the default visible state, and `game.js` now shows the polling path calling `renderOnlineStatus()` instead of rebuilding the whole loadout screen. Local Spring Boot was restarted from `spring-boot-23.log` and now serves the same updated markup on `http://localhost:8080`.
- March 29, 2026 deployment fix: the hosted app could land on the empty board shell when the initial `/api/game/options` request stalled, because `loadGameOptions()` only opened the loadout overlay after the request completed. `src/main/resources/static/js/game.js` now opens the loadout overlay immediately on startup and wraps JSON fetches in a 10-second timeout so stalled backend requests turn into a retryable error instead of a blank board.
- Temporary live backend restored via localhost.run tunnel `https://3c70a95549c871.lhr.life`, and the Firebase temp Hosting bundle in `.deploy-hosting/static` was refreshed + redeployed. Verified on March 29, 2026 that hosted `config.js` points to that tunnel, hosted `game.js` includes `DEFAULT_REQUEST_TIMEOUT_MS` and the immediate `overlay.classList.add('visible')` startup path, `GET /api/game/options` returns `200`, and `POST /api/game/new` returns a live game state.
- Mobile layout follow-up on March 29, 2026: reworked the compact layout so the enemy/player summary cards stay side by side at the top, the board gets a taller centered lane, and the action buttons dock to a fixed bottom action bar on mobile. Hand cards were rebuilt into a full-card shell so notch dots sit on the actual card perimeter instead of a mini preview box, and selected cards can now show creature art.
- Added a repo-local Sundile art asset at `src/main/resources/static/assets/cards/sundile.svg`, mapped that art in `src/main/resources/static/js/game.js`, and mirrored the updated `index.html`, `style.css`, `game.js`, `firebase-init.js`, and Sundile SVG into `.deploy-hosting/static` without overwriting the tunnel-backed `.deploy-hosting/static/js/config.js`.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes, Firebase Hosting redeployed successfully to `https://siegelingstcgtesting.web.app`, hosted `index.html` now serves `style.css?v=3` and `game.js?v=3`, hosted `config.js` still points to `https://prices-acids-regions-atom.trycloudflare.com`, and `https://siegelingstcgtesting.web.app/assets/cards/sundile.svg` returns `200 image/svg+xml`.
- March 29, 2026 deck builder UX update: added a dedicated card preview panel to the builder layout. Clicking a card in the catalog or the chosen deck list now opens a full preview showing the card frame, art/fallback art, notches for Sieglings, and card metadata/effect text. The preview follows the visible card set when filters change and highlights the active row in both the catalog and deck list.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes, Firebase Hosting redeployed successfully, hosted `index.html` now serves `style.css?v=4` and `game.js?v=4`, hosted markup includes `builderCardPreview`, hosted `config.js` uses same-origin `apiBaseUrl: ''`, and hosted `GET /api/game/options` still returns `200`.
- March 29, 2026 preview-card polish: centered the preview card title/header block, removed the auto-push that was pinning stats/effect copy against the bottom edge, and added extra bottom padding so HP/stats/cost text sits just under the white art panel instead of colliding with the lower notch row.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes, Firebase Hosting redeployed successfully, hosted `index.html` now serves `style.css?v=5`, and hosted `GET /api/game/options` still returns `200`.
- March 29, 2026 small-phone layout pass: added a second compact mobile breakpoint for narrow or short screens. The gameplay header, summary cards, board, utility panel, and fixed action bar now scale down further, and the hand switches from a multi-row grid into a short horizontal swipe strip so the full game stays on a single mobile screen more reliably.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes, Firebase Hosting redeployed successfully, hosted `index.html` now serves `style.css?v=6`, and hosted `GET https://siegelingstcgtesting.web.app/api/game/options` still returns `200` with `10` decks and `8` trainers. Playwright/mobile screenshot validation is still blocked because `node`/`npx` are not available in this environment.
- March 29, 2026 mobile usability follow-up: for the smallest phone breakpoint, the page is scrollable again, the fixed action bar is returned to normal flow, the enemy and player boards render side by side instead of stacked vertically, and the hand is now sticky at the bottom so placement cards remain reachable while playing.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes, Firebase Hosting redeployed successfully, hosted `index.html` now serves `style.css?v=7`, and hosted `GET https://siegelingstcgtesting.web.app/api/game/options` still returns `200` with `10` decks and `8` trainers. Automated mobile screenshot validation is still blocked because `node`/`npx` are not available in this environment.
- March 29, 2026 board-side readability tweak: enemy board slots now use a red outline/tint and the player board slots keep a blue outline/tint, with matching red and blue board-side labels for faster read during play.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes, Firebase Hosting redeployed successfully, hosted `index.html` now serves `style.css?v=8`, and hosted `GET https://siegelingstcgtesting.web.app/api/game/options` still returns `200`.
- March 29, 2026 arena socket rework: external energy no longer comes from any notch pointing off-map. `EnergyService` now only awards external energy from explicit perimeter sockets: left-column `LEFT` notches, right-column `RIGHT` notches, and the board's outer horizontal edge (`BOTTOM` on the player board, `TOP` on the enemy board). Diagonal outward notches no longer generate energy.
- Frontend board rendering now draws nine circular perimeter sockets per board (left 3, right 3, outer edge 3), colors active sockets to the linked element, and draws short edge-to-socket links. Board notch highlighting now marks only those socket-valid outward notches as `external`.
- Latest verification: `node --check src/main/resources/static/js/game.js` passes via the packaged Node binary, `.\mvnw.cmd -q -DskipTests package` passes, local Spring Boot was launched from `target\sieglings-tcg-0.1.0-SNAPSHOT.jar` and returns `200` for `http://127.0.0.1:8080/api/game/options`, and the fallback public backend tunnel `https://727329134307da.lhr.life` returns `200` for both `/api/game/options` and `/api/game/new`.
- Latest deploy state: Firebase Hosting redeployed successfully to `https://siegelingstcgtesting.web.app`, hosted `index.html` now serves `style.css?v=12` and `game.js?v=9`, and hosted `js/config.js` temporarily points `apiBaseUrl` to `https://727329134307da.lhr.life` because the Cloud SDK in this shell has no active authenticated account for `gcloud run deploy`. Swap `src/main/resources/static/js/config.js` back to same-origin `apiBaseUrl: ''` after the next authenticated Cloud Run deploy.
- March 29, 2026 opening flow/account pass: added a database-backed account system using Spring Data JPA + file-backed H2 (`data/sieglings.mv.db`). New endpoints now support email register/login/logout plus `/api/auth/me`, saved deck CRUD, and recent match history reads. Passwords are hashed with BCrypt and auth uses bearer tokens stored in the database.
- Added opening-hand mulligans to game startup. New matches now begin in `MULLIGAN`, solo AI resolves its mulligan automatically, multiplayer waits for both players, and `POST /api/game/mulligan` advances the game into the first draw phase once both sides lock their hands.
- Added a new welcome screen in `src/main/resources/static/index.html` and `src/main/resources/static/js/game.js` with a 4-slide visual tutorial covering placement, link formation, socket energy, and setup-vs-battle energy pacing. The loadout screen now sits behind that welcome flow instead of appearing immediately on startup.
- Added account-aware saved deck UI inside the loadout overlay. Logged-in players can save the current preset or custom builder loadout with a name, reload saved lists later, and start games with that saved loadout label so future match history can show the deck name.
- Added a mulligan overlay/UI flow in `src/main/resources/static/index.html`, `src/main/resources/static/js/game.js`, and `src/main/resources/static/css/style.css` so the player sees the current opening hand and can choose `Keep Hand` or `Mulligan` once.
- Backend verification: `.\mvnw.cmd -q -DskipTests compile` and `.\mvnw.cmd -q -DskipTests package` both pass after stopping the old jar lock. Local app is currently running from `spring-boot-auth.log` / `spring-boot-auth.err` on port `8080`.
- Live local API verification on March 29, 2026:
  - `POST /api/auth/register` returns `authenticated: true` and a bearer token.
  - `POST /api/profile/decks` saves a named deck and it survives logout/login, confirming DB persistence.
  - `POST /api/game/new` now returns `currentPhase: "MULLIGAN"` with `mulligan.active: true`.
  - `POST /api/game/mulligan` with `takeMulligan=false` moves the game into `DRAW`.
  - Local HTML contains the new `welcomeOverlay`, `mulliganOverlay`, and `savedDecksSection` markers.
- The Playwright loop from the `develop-web-game` skill still could not be executed end-to-end in this shell because the skill script depends on the `playwright` npm package, which is not installed here. Verification for this pass was done via syntax check, Maven build, and live HTTP endpoint checks against the local running app.
- March 30, 2026 backend socket regression pass: `EnergyService` now models outside energy through explicit perimeter socket keys before tallying totals, so the backend matches the UI's actual socket-point model instead of counting raw off-board notches. Added `src/test/java/com/sieglings/service/EnergyServiceTest.java` coverage for the reported enemy front-left `Clawkid` case and for a diagonal-only enemy front-left notch, both confirming that diagonals do not create extra socket energy.
- March 30, 2026 compile cleanup: added `spring-boot-starter-test` to `pom.xml` and fixed enum switch expressions that broke full compile/test runs after the extra placeholder elements were added in `CardDefinitionService`, `EffectService`, `GeneratedSpellCatalog`, and `GeneratedCreatureCatalog`.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes and `.\mvnw.cmd -q test` passes from `A:\New folder\OneDrive\Desktop\Sieglings\sieglings-tcg`.
- March 30, 2026 disconnected-energy fix: `EnergyService` now analyzes only the active connected foundation network by reusing `PlacementService.getFoundationSieglings(...)`. This means movement abilities that break reciprocal links now immediately drop the energy gained from those broken links after battle-phase recalculation.
- Added a move regression in `src/test/java/com/sieglings/service/EnergyServiceTest.java` that moves a linked Siegling to an isolated point and verifies the broken internal energy disappears after `recalculateEnergy`.
- Latest verification: `.\mvnw.cmd -q test` passes after the disconnected-energy fix.
- March 30, 2026 preset deck rebalance: `CardDefinitionService` now builds preset decks with an exact `20 Sieglings / 10 spells / 10 traps` structure instead of dumping the whole element pool. Siegling packages are assembled from full evolution lines only, never exceed 3 copies per card, and distribute the 20-creature target across the deck's included elements.
- Spell and trap packages now use fixed-size repeated bundles that prefer lower-cost, lower-combo, on-theme options first, which makes the preset lists less clogged with expensive setup cards.
- Added `src/test/java/com/sieglings/service/CardDefinitionServiceTest.java` to verify every preset deck option builds to 40 cards total, contains the exact `20 / 10 / 10` split, respects the 3-copy limit, and includes full evolution trees whenever any member of a Siegling line appears.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes and `.\mvnw.cmd -q test` passes after the preset deck rebalance.
- March 30, 2026 stat-system simplification: Sieglings now expose only `Health`, `Speed`, notches, and abilities. The old printed `Attack` and `Defense` stats were removed from `SieglingCard`, `CardInstance`, API serialization, and the frontend card/board/tooltips.
- Damage now comes from battle abilities and printed attack abilities instead of a permanent attack stat. Temporary offense effects were reshaped into `damage_boost`, while old defense-style buffs were converted into `health_boost` so durability is expressed through Health instead of hidden damage reduction.
- Trainer rosters, generated creatures, and generated spells were updated so their text/effects now reference attack-damage boosts, health boosts, healing, speed, freeze, and destroy effects instead of `Attack/Defense` stat buffs.
- Latest verification: `.\mvnw.cmd -q test` passes after the stat-system simplification. A direct `node --check` pass could not be run in this shell because `node` is not installed.
- March 30, 2026 card-render cleanup: the live `/js/game.js` bundle now formats Siegling stat lines defensively so missing values no longer print `undefined` chips on hand, board, preview, selected-card, or tooltip surfaces. Board stat badges and HP bars now render only from numeric values.
- `Staticap.png` was confirmed to exist and load from `/images/cards/Staticap.png`, but the file is a full-card scan rather than a cropped illustration. The renderer now treats that asset as an illustration-crop source, zooming/focusing the artwork pane so the old embedded frame/stat area does not dominate the card face.
- Cache-bust versions were advanced in `src/main/resources/static/index.html` to `style.css?v=16` and `game.js?v=15`.
- Latest verification: `.\mvnw.cmd -q -DskipTests compile` passes after the card-render cleanup. Playwright/browser screenshot validation is still blocked in this shell because the packaged `node` binary is not present.
- March 31, 2026 placement-anchor update: non-evolution Sieglings can now be placed using either a reciprocal creature-notch link or a compatible perimeter socket on that empty cell. Left-edge cells accept `LEFT`, right-edge cells accept `RIGHT`, and the board's outer row accepts `BOTTOM` for the player side or `TOP` for the enemy side.
- `PlacementService.getFoundationSieglings(...)` now treats externally anchored edge cards as valid foundation seeds too, so cards placed via sockets remain live for energy generation and future expansion instead of becoming dead isolated pieces.
- Frontend placement previews in `src/main/resources/static/js/game.js` now mirror that rule by allowing highlighted legal cells when the selected Siegling can anchor to an open external socket, not only when it can link to a neighboring creature.
- Added `src/test/java/com/sieglings/service/PlacementServiceTest.java` coverage for socket-only placement legality and for externally anchored cards remaining in the foundation network. Updated `EnergyServiceTest` so the broken-link movement regression still checks a truly unanchored destination under the new rule.
- Latest verification: `.\mvnw.cmd -q test` passes after the placement-anchor update.
- March 31, 2026 manual card-tuning system: Siegling stats, notches, cost, evolution data, and abilities can now be overridden through `src/main/resources/cards/siegling-overrides.json` without hand-rewriting the generated roster. `GeneratedCreatureCatalog` still supplies the baseline cards, and `ManualSieglingCatalog` merges any listed overrides on top.
- Added `CARD_TUNING.md` with the editable JSON shape and an example override, plus `ManualSieglingCatalogTest` coverage proving a hand-authored definition can replace generated rarity, health, speed, notches, cost, and ability fields.
- March 31, 2026 ability effect-key registry: added `AbilityEffectKeys` plus `ABILITY_EFFECT_KEYS.md` so ability definitions and manual override JSON have a single reference for supported effect strings instead of relying on scattered magic values.
- Added `connected_allies_health_boost`, a new source-based effect that gives max Health to allied Sieglings connected to the source card's live reciprocal-link network. `EffectService` now resolves it from the source card, `BattleService` preserves sensible self-targeting when passive creature abilities are converted into active battle abilities, and `EffectServiceTest` covers the linked-vs-isolated ally case.
- March 31, 2026 connected-effect expansion: added `connected_allies_damage_boost` alongside the connected-health effect so link-network abilities can now buff either attack damage or max Health on connected allies only. `ABILITY_EFFECT_KEYS.md` documents both, and `EffectServiceTest` now covers both families.
- March 31, 2026 connected-effect expansion follow-up: added `connected_allies_speed_boost` so connected-network abilities can now buff attack damage, max Health, or Speed on linked allies only. `BattleService` now treats all three connected buff keys as source-based self-target abilities, and `EffectServiceTest` covers the speed variant too.
- March 31, 2026 override JSON ordering pass: reordered `src/main/resources/cards/siegling-overrides.json` by element, then grouped full evolution lines together within each element, with standalone creatures listed after the evo groups for easier manual tuning.
- March 31, 2026 multi-ability override support: Sieglings can now carry an explicit `abilities` array in `siegling-overrides.json`, with each ability defining its own `requiredElement` and `requiredEnergy`. `BattleService` treats that array as the card's explicit battle loadout, preserving per-ability costs instead of overwriting them with rarity defaults, and the frontend card views now render ability lists instead of assuming a single printed effect.
- April 1, 2026 frontend restore pass: brought back Claude's mobile-first gameplay shell from `origin/claude/update-card-energy-costs-cknt0` by restoring `src/main/resources/static/index.html`, `src/main/resources/static/css/style.css`, and `src/main/resources/static/js/game.js`, while keeping the current Firestore/dashboard stack intact.
- Follow-up adjustments on the restore: re-added a direct `/card-dashboard.html` shortcut to the action bar, restored the newer card-art crop metadata flow for illustration assets like `Staticap`, removed the stale preview-float `ATK` stat chip, and bumped the live game asset versions to `style.css?v=23` and `game.js?v=21` to force a clean refresh after deploy.
- Browser-pass cleanup on the same restore: removed the old optimistic PNG auto-probe fallback from `src/main/resources/static/js/game.js` because it was generating 404 console noise for unmapped cards like `Applehead.png` during page load. Known art now comes from the explicit card-art registry only.
- Verification for the restore on April 1, 2026: `.\mvnw.cmd -q test` passes, `node --check src/main/resources/static/js/game.js` passes via the portable Node install, the `develop-web-game` Playwright client now runs locally after installing `playwright` under `C:\Users\AlexTillman\.codex\skills\develop-web-game`, and the mobile browser flow screenshots in `output/web-game/restore-mobile-flow` confirm the welcome screen, loadout screen, and in-match mobile shell with the Claude segmented action bar.
- Deploy verification for the same pass: Firebase Hosting redeployed successfully to `https://siegelingstcgtesting.web.app` and Cloud Run redeployed successfully as revision `sieglings-tcg-api-00018-v4j` at `https://sieglings-tcg-api-324063847257.us-central1.run.app`. Live HTML on both endpoints now serves the restored `top-bar` + `action-bar` layout with `style.css?v=23` and `game.js?v=21`, and `https://siegelingstcgtesting.web.app/api/cards/editor` still reports `source: FIRESTORE`, `liveEditingEnabled: true`, and `firestoreAvailable: true`.
- April 1, 2026 PC layout follow-up: desktop gameplay now swaps to a side-by-side enemy/player board layout at `min-width: 980px`, trims the desktop action bar/hand tray footprint, and serves `style.css?v=24` so the full board stays visible on normal PC windows instead of being cropped by the bottom tray.
- Same follow-up: restored Claude's action-bar preview control by replacing the temporary `DB` shortcut with the selected-card preview button (`openDrawer('selected')`). The card dashboard remains available directly at `/card-dashboard.html`, but it is no longer occupying the in-match quick-action slot.
- Latest verification for that follow-up: `.\mvnw.cmd -q test` passes, local Playwright screenshots in `output/web-game/layout-regression-check` confirm the full board is visible at `1366x768` and the mobile action row shows the eye preview button again, Firebase Hosting redeployed successfully to `https://siegelingstcgtesting.web.app`, Cloud Run redeployed successfully as revision `sieglings-tcg-api-00019-hch`, and the live editor API still reports `source: FIRESTORE`, `liveEditingEnabled: true`, and `firestoreAvailable: true`.
- April 1, 2026 dashboard editor expansion: the card dashboard now has two editing pages, with a dedicated `Spell / Trap Page` alongside the original Siegling page. The browser, editor form, validation, and export flow now understand `SIEGLING`, `SPELL`, and `TRAP` records, including spell play-cost/reaction/combo fields and trap trigger-element/trigger-amount fields.
- Backend follow-up for the same pass: `ManualSieglingCatalog` now exports/imports mixed card definitions instead of Siegling-only rows, `CardDefinitionService` applies live spell/trap overrides on top of generated bases, and `CardOverrideEditorService` now seeds the dashboard from the full current deck-builder catalog so Firestore editing can round-trip all three card types.
- Verification for the mixed-card dashboard pass: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/card-dashboard.js` passes, `.\mvnw.cmd -q test` passes, local `GET http://127.0.0.1:8080/api/cards/editor` returns `source: FIRESTORE` plus `metadata.cardTypes = [SIEGLING, SPELL, TRAP]`, and local Playwright smoke checks in `output/dashboard-spelltrap-smoke` confirmed the Spell / Trap page loads, switches to a spell editor, and can create a new trap draft with the correct trap-only fields visible.
- April 1, 2026 premade deck dashboard expansion: added a third `Premade Decks` page to `src/main/resources/static/card-dashboard.html` and `src/main/resources/static/js/card-dashboard.js`. The dashboard can now browse the current preset deck catalog, create/duplicate/delete deck entries, edit deck identity and recommended trainer, add/remove cards from a deck list, and toggle whether each premade deck is active in the live game loadout chooser.
- Backend follow-up for the same pass: added `src/main/java/com/sieglings/service/PresetDeckCatalogService.java` to persist preset deck definitions alongside the card catalog, updated `CardOverrideEditorService` to round-trip both `cards` and `decks` in one Firestore-backed payload, and updated `CardDefinitionService`, `GameService`, and `GameController` so `/api/game/options` only exposes active premade decks while `buildDeckById(...)` can also honor explicit saved `cardIds` lists.
- Verification for the premade deck pass: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/card-dashboard.js` passes, `.\mvnw.cmd -q test` passes with new `CardDefinitionServiceTest` coverage for inactive-deck filtering and explicit preset deck lists, local `GET http://127.0.0.1:8080/api/cards/editor` returns `source: FIRESTORE` with `data.decks`, `metadata.trainers`, and `metadata.deckRules`, local `GET http://127.0.0.1:8080/api/game/options` returns `24` active deck options with dynamic defaults, and local served `card-dashboard.html` now includes the `Premade Decks` tab plus `deckWorkspace` / `Premade Deck Editor` markup.
- April 2, 2026 battle-action UX pass: added a `Pass` option to the battle action drawer so each acting Siegling can skip its turn cleanly, and the player-facing action list now sorts abilities by ascending energy cost before rendering.
- Same pass: added `getAbilityRequiredEnergy`, `formatBattleAbilityCost`, and `getSortedBattleAbilities` in `src/main/resources/static/js/game.js`, plus a muted `.battle-pass-btn` style in `src/main/resources/static/css/style.css`.
- Validation blocker discovered and fixed during the same pass: older live override rows loaded from Firestore can omit `type`, so simple spell ids like `spell_fire_06` were being inferred as Sieglings and crashing `/api/game/options`. `ManualSieglingCatalog.definitionType(...)` now infers `SPELL`/`TRAP` from legacy id prefixes before falling back to field heuristics, and `ManualSieglingCatalogTest` now covers the untyped `spell_fire_06` case.
- Latest verification: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests compile` passes, `.\mvnw.cmd -q test` passes, local `GET http://127.0.0.1:8080/api/game/options` returns `200` again, the required `develop-web-game` Playwright client captured the guest->loadout screen in `output/web-game/pass-action-client-check/shot-0.png`, and a focused Playwright smoke check in `output/web-game/pass-action-panel-check/battle-pass-panel.png` confirmed the battle drawer orders abilities as `Free / 1 / 2 / 3` energy and that the new `Pass` button submits `abilityIndex: -1`.
- April 2, 2026 move-targeting drawer polish: selecting a targeted battle ability now immediately collapses the battle drawer instead of leaving the mobile tray/backdrop on top of the board. `src/main/resources/static/js/game.js` now has `isBattleTargetSelectionActive()`, `closeDrawer(immediate)`, and a guard in `renderBattlePanel()` so the drawer does not auto-reopen while battle targeting is active.
- Same pass: `chooseBattleAbility(...)` now calls `closeDrawer(true)` after entering battle target mode, which makes move abilities usable in one tap instead of forcing an extra tap just to dismiss the tray.
- Latest verification: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests package` passes, `.\mvnw.cmd -q test` passes, local `GET http://127.0.0.1:8080/api/game/options` returns `200`, the required `develop-web-game` Playwright client captured the guest->loadout screen in `output/web-game/move-target-client-check/shot-0.png`, and a focused Playwright harness in `output/web-game/move-target-panel-check/move-targeting.png` confirmed `targetMode: true`, `activeDrawer: null`, `battleDrawerHidden: true`, and `backdropHidden: true` immediately after selecting a move ability.
- April 2, 2026 SiegeKnight action-bar pass: added a fourth action-row button for player SiegeKnight abilities when the active ability is currently usable. The button is hidden when no active trainer ability is available, and it now opens a dedicated popup instead of relying on the hidden trainer panel.
- Same pass: added `trainerAbilityOverlay` / `trainerAbilityModal` markup in `src/main/resources/static/index.html`, popup state/render helpers in `src/main/resources/static/js/game.js`, and popup styling plus the new `.ab-trainer` action-row color in `src/main/resources/static/css/style.css`. The popup shows the active ability name, its description, a short targeting/usage hint, and a `Use Ability` / `Choose Target` confirmation button that launches the existing trainer ability flow.
- Latest verification: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q test` passes, `.\mvnw.cmd -q -DskipTests package` passes, local `GET http://127.0.0.1:8080/api/game/options` returns `200`, the required `develop-web-game` Playwright client captured the guest->loadout screen in `output/web-game/trainer-popup-client-check/shot-0.png`, and a focused Playwright harness in `output/web-game/trainer-popup-panel-check/trainer-popup.png` confirmed the row button is visible, the popup renders the active ability title/description/hint, and the CTA label changes to `Choose Target` for targeted SiegeKnight abilities.
- April 2, 2026 battle readability pass: moved SiegeKnight active resets out of `draw(...)` so trainer actives now refresh only when the battle phase fully completes and the next round begins. Added `GameServiceTest` coverage to lock that timing in.
- Same pass: repurposed the main `Battle` action-row button into a real battle-status control. Outside battle it now reads `Auto Battle` and explains that battle starts automatically after both setup turns; during battle it changes to labels like `Act Now` / `Enemy Acts` and opens the battle queue drawer instead of behaving like a dead disabled indicator.
- Same pass: scaled the desktop action row up substantially in the `min-width: 980px` layout, added a red battle-phase arena treatment across platforms, and upgraded the active speed card highlight with an `Acting` badge plus a stronger aura glow so both players can read the current battle actor immediately.
- April 2, 2026 hand-scale + arena-theme pass: enlarged the in-match hand tray cards from miniature chips into near full-card previews on desktop (`~152px` card width in-browser), while keeping the smaller mobile breakpoints intact. Updated the tray padding, hover lift, title sizing, and art height in `src/main/resources/static/css/style.css`.
- Same pass: replaced the fixed battle-only red wash on the arena with a live element-driven theme. `src/main/resources/static/js/game.js` now derives up to four weighted element colors from the two trainers plus both current boards and pushes them into CSS custom properties on `#boardArea` during `render()`. `src/main/resources/static/css/style.css` now uses those variables for layered drifting glow and clash overlays, with an intensified red-tinted version during `BATTLE`.
- Latest verification for the hand-scale + arena-theme pass: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests compile` passes, the required `develop-web-game` Playwright client captured the guest->loadout/start flow in `output/web-game/arena-theme-client-check/shot-0.png`, and a focused Playwright harness in `output/web-game/arena-theme-focused-check` confirmed `firstCardWidth: 152` with screenshots for both `hand-scaled.png` and `arena-battle-theme.png` plus a clean `state.json` and no console errors.
- April 2, 2026 action-row visibility cleanup: the main `Battle` button now stays hidden outside the battle phase, and the `Draw` button now hides while battle is active so the action row only shows context-relevant controls. This logic lives in `src/main/resources/static/js/game.js` inside `render()`.
- Latest verification for the action-row cleanup: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests compile` passes, the required `develop-web-game` Playwright client captured the startup flow in `output/web-game/action-row-visibility-client-check/shot-0.png`, and a focused Playwright harness in `output/web-game/action-row-visibility-focused-check` confirmed `DRAW => drawHidden:false/battleHidden:true` and `BATTLE => drawHidden:true/battleHidden:false` with screenshots and no console errors.
- April 2, 2026 hand-hover lift pass: desktop hand cards can now rise over the action bar instead of feeling clipped under it. `src/main/resources/static/css/style.css` now gives the tray a reserved hover lane, raises the hover translation to `-92px` on desktop, and lets the hovered card stack above the `Draw / Knight / End Turn` row with a stronger drop shadow.
- Same pass: removed the old lock-state hover suppression on `.hand-card.interaction-locked:hover` and `.hand-card.opening-locked:hover`, so even unavailable cards can still be lifted and inspected without implying they are playable.
- Latest verification for the hand-hover lift pass: `.\mvnw.cmd -q -DskipTests compile` passes, the required `develop-web-game` Playwright client captured the startup flow in `output/web-game/hover-lift-client-check/shot-0.png`, and a focused Playwright harness in `output/web-game/hover-lift-focused-check-4` confirmed `transform: matrix(1.06, 0, 0, 1.06, 0, -92)`, `overlapsActionBar: true`, and `risesAboveBarTop: true` with the visual result in `hover-lift.png`.
- April 2, 2026 hand-hover lift correction: removed the always-overlapping desktop tray offset so the hand row sits fully below the action bar at rest again. The overlap is now caused only by the hovered or selected card's own transform, not by the tray/container layout.
- Latest verification for the hover-lift correction: `.\mvnw.cmd -q -DskipTests compile` passes, and the focused Playwright harness in `output/web-game/hover-lift-restored-check` confirmed `resting.overlapsActionBar: false` while `hovered.overlapsActionBar: true` and `hovered.risesAboveBarTop: true`, with screenshots in `resting-row.png` and `hovered-row.png`.
- April 2, 2026 dynamic hand-lift layer: replaced the pure-CSS overlap trick with a dedicated `#handLiftLayer` overlay in `src/main/resources/static/index.html` plus JS lift-state management in `src/main/resources/static/js/game.js`. Hovered or selected desktop hand cards are now cloned into that fixed overlay and positioned above the HUD area, while the original source card is dimmed in-place to preserve the row layout.
- Same pass: hand hover now goes through `handleHandCardPointerEnter/Leave(...)`, `updateHandLiftLayer()`, and `getLiftedHandCardId()`. Only one lifted card is shown at a time, with hover taking precedence over selection; mobile still skips the lift layer and uses the existing inspector flow.
- Latest verification for the dynamic hand-lift layer: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests compile` passes, and the focused Playwright harnesses in `output/web-game/hand-lift-layer-check` and `output/web-game/hand-lift-targeted-check` confirmed `liftLayerVisible: true`, `liftCloneCount: 1`, and a lifted center card / selected setup card with screenshots in `hover-center-card.png` and `selected-center-card.png`.
- Latest verification: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `./mvnw.cmd -q -DskipTests compile` passes, `./mvnw.cmd -q test` passes, local packaged jar is running from `spring-boot-battle-ux.log` on `http://127.0.0.1:8080`, the required `develop-web-game` client captured the guest->loadout flow in `output/web-game/battle-ux-client-check`, and the focused Playwright UI harness in `output/web-game/battle-ux-focused-check` confirmed `battle-phase-active`, `Act Now`, an `86px` desktop action bar, a `56px` main button height, the red arena background, and the active-attacker badge/aura. The focused browser check injects a mock battle snapshot after bootstrapping the app so the UI can be validated deterministically without relying on a random opening hand.
- April 2, 2026 placement-anchor mismatch fix: the frontend was correctly highlighting Emberfox's middle placement because it can reciprocally link to the friendly Pylook above, but the backend still limited non-socket anchor checks to the current foundation cluster. `PlacementService.isConnectedByAnchor(...)` now considers any friendly Siegling already on the board for reciprocal notch anchoring, which keeps server placement legality aligned with the UI's legal-cell highlight behavior.
- Added `PlacementServiceTest.placementCanAnchorOffDisconnectedFriendlySiegling()` to lock in the reported case: a disconnected friendly card with a matching notch can still serve as the legal anchor for a new placement even when it is outside the current foundation network.
- Latest verification for the placement-anchor fix: `./mvnw.cmd -q -DskipTests compile` passes and `./mvnw.cmd -q test` passes from `A:\New folder\OneDrive\Desktop\Sieglings\sieglings-tcg`.
- April 2, 2026 loadout start-bar polish: the chooser footer now uses a solid sticky action tray instead of a transparent gradient edge, so the deck/trainer cards no longer visually peek underneath the `Start Match` area while the overlay scrolls. The tray is now a full-width anchored panel with its own border/shadow, and the primary CTA uses a dedicated `loadout-start-btn` style.
- Same pass: added a one-shot `loadoutStartPending` state in `src/main/resources/static/js/game.js` so `Start Match` / `Create Room` / `Join Room` immediately flips into a disabled loading label (`Starting Match...`, `Creating Room...`, etc.) with `aria-busy=true`, preventing rapid duplicate submissions while the request is in flight.
- Latest verification for the loadout start-bar pass: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `./mvnw.cmd -q -DskipTests compile` passes, the local app is running from `spring-boot-loadout-footer.log` on `http://127.0.0.1:8080`, the required `develop-web-game` client captured the chooser in `output/web-game/loadout-start-footer-check/shot-0.png`, and a focused Playwright harness in `output/web-game/loadout-start-button-focused-check` confirmed the new hard tray plus `Starting Match...`, `disabled: true`, `is-loading: true`, and `ariaBusy: "true"` immediately after clicking the CTA.
- April 2, 2026 start-match timeout fix: the browser was never reaching mulligan because the frontend's generic `DEFAULT_REQUEST_TIMEOUT_MS` of 10 seconds was aborting `/api/game/new` before the backend finished assembling the opening match state. A raw browser fetch measured the real response at about 18.6 seconds locally, so the request itself was healthy but our own timeout killed it.
- Added `LOADOUT_ACTION_TIMEOUT_MS = 30000` in `src/main/resources/static/js/game.js` and wired it into `newGame()`, `createRoom()`, and `joinRoom()` so the loadout CTA keeps its loading lock long enough for the full match-start response to return.
- Latest verification for the timeout fix: `node --check` and `./mvnw.cmd -q -DskipTests compile` pass, and the focused Playwright browser run in `output/web-game/loadout-start-button-focused-check/deep-start-after-fix.json` now shows a successful `/api/game/new` `200` response followed by `visibleLoadout: false`, `mulliganVisible: true`, and `gameStatePhase: "MULLIGAN"` on the local app.
- April 3, 2026 mobile landscape repair: fixed the compact landscape breakpoint so the board cells keep a real aspect ratio instead of collapsing into thin strips when the phone rotates. The compact landscape arena now gives each half a fixed board width, restores board-cell height, and hides the floating card preview there so it no longer covers the battlefield.
- Same pass: aligned `isMobileLayout()` in `src/main/resources/static/js/game.js` with the compact landscape breakpoint so JS behavior matches the CSS layout after rotation, and tightened the low-height mulligan/welcome/loadout overlays so their content scrolls correctly and the mulligan `Keep hand` button stays on-screen.
- Latest verification for the landscape repair: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests compile` passes, the required `develop-web-game` Playwright client captured the guest->loadout flow in `output/web-game/mobile-landscape-client-check`, and focused mobile-landscape browser checks in `output/web-game/mobile-landscape-broken-check`, `output/web-game/mobile-landscape-fixed-check`, and `output/web-game/mobile-landscape-mulligan-check` confirmed the board grids grew from `32px` to about `301px` tall at `844x390`, the preview float no longer overlays the arena in that breakpoint, and the mulligan `Keep hand` button remained visible inside the viewport.
- April 3, 2026 Siegling claim + max-cap pass: hand Sieglings now show `Maxed out.` once your board already holds 5 Sieglings, instead of falling through to the generic no-placement copy. Player-board Sieglings that have survived at least 1 full battle phase now render a `Claim` badge during Setup and can be claimed by tapping their board cell.
- Backend follow-up for the same pass: added `battlePhasesSeen` tracking to `CardInstance` (and preserved it through evolution), a new `POST /api/game/claim` action through solo + multiplayer, and temporary per-element energy adjustments on `Player` so claims add 1 one-turn energy of the claimed Siegling's element without wiping prior setup-phase spending when the board recalculates. Setup turn end / battle start / new round now clear those temporary adjustments.
- Latest verification for the claim pass: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q test` passes, `.\mvnw.cmd -q -DskipTests package` passes, the required `develop-web-game` Playwright client ran against `http://127.0.0.1:8080` with captures in `output/web-game/claim-client-check` and `output/web-game/claim-client-check-2`, and a fresh packaged app on `http://127.0.0.1:8081` was browser-checked with the focused mock-state harness in `output/web-game/claim-feature-live-check`, confirming `claimPromptCount: 5`, `claimableCellCount: 5`, `handLockCopy` containing two `Maxed out.` entries, and `placementActive: false` with the `Claim` interaction banner visible in `claim-ui.png`.
- April 3, 2026 claim confirmation follow-up: player-board claim taps no longer fire the sacrifice immediately. `src/main/resources/static/index.html` now includes a dedicated `claimConfirmOverlay` modal, `src/main/resources/static/js/game.js` keeps a `pendingClaimTarget` plus `openClaimPopup(...) / confirmClaimFromPopup()` flow, and claim clicks from `renderBoard(...)` now open the popup first instead of calling `claimBoardCard(...)` directly. Escape/reset/end-turn/battle-entry paths also close the popup cleanly.
- Same follow-up: `src/main/resources/static/css/style.css` now gives the claim modal its own green-accent variant, and `index.html` cache-bust versions were advanced to `style.css?v=31` and `game.js?v=30` so the running app on `http://127.0.0.1:8080` serves the new popup markup immediately.
- Latest verification for the claim confirmation follow-up: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q test` passes, `.\mvnw.cmd -q -DskipTests compile` passes, the required `develop-web-game` client captured startup flow in `output/web-game/claim-popup-client-check`, and a focused Playwright harness in `output/web-game/claim-popup-modal-check` confirmed a claimable board-card tap opens `Claim Emberfox?`, leaves the board unchanged until confirmation, then sends `POST claim { row: 2, col: 0 }` and removes only that claimed board card. `.\mvnw.cmd -q -DskipTests package` currently fails because the existing `target/sieglings-tcg-0.1.0-SNAPSHOT.jar` is locked by a running process.
- April 3, 2026 placement-highlight follow-up: the frontend socket-anchor helper in `src/main/resources/static/js/game.js` already required the real outward notch direction, but the running browser session could still keep the older `v=31` asset cached and show illegal `+ Place` highlights like the reported Firsky back-left case. `src/main/resources/static/index.html` now bumps the live asset versions to `style.css?v=32` and `game.js?v=32` so the next refresh is forced onto the corrected placement bundle.
- April 3, 2026 phase-transition UX pass: the battle queue now auto-closes as soon as the game leaves `BATTLE`, so players can draw immediately without manually dismissing the drawer after combat. Added a new full-width `phaseTransitionBanner` overlay in `src/main/resources/static/index.html`, transition helpers in `src/main/resources/static/js/game.js`, and dramatic gold-tinted phase banner styling in `src/main/resources/static/css/style.css` so phase changes announce `Draw Phase`, `Setup Phase`, and `Battle Phase` in a screen-spanning bar. Asset versions were advanced again to `style.css?v=33` and `game.js?v=33`.
- April 3, 2026 start-match backend latency follow-up: `POST /api/game/new` was still missing the frontend's 30-second loadout timeout in local testing because the Firestore-backed live config snapshots for card overrides, preset decks, and trainers were expiring after only 5 seconds. `CardOverrideStorageService`, `PresetDeckCatalogService`, and `TrainerCatalogService` now keep their remote snapshot caches warm for 5 minutes instead, so opening the loadout screen and then clicking `Start Match` reuses the same live config instead of forcing another remote fetch.
- Latest verification for the start-match latency follow-up: `.\mvnw.cmd -q -DskipTests compile` and `.\mvnw.cmd -q -DskipTests package` pass. After restarting the local app on `http://127.0.0.1:8080`, a fresh `GET /api/game/options` still took about `10.7s` on cold start, but a subsequent `POST /api/game/new` now completed in about `26.1s` and returned `currentPhase: "MULLIGAN"`, `mulligan.active: true`, `youPending: true`, and a 5-card opening hand instead of hanging past the browser timeout.
- Latest verification for the phase-transition UX pass: `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, `.\mvnw.cmd -q -DskipTests compile` passes, the required `develop-web-game` client captured startup flow in `output/web-game/phase-banner-client-check`, and a focused Playwright harness in `output/web-game/phase-transition-focused-check` confirmed `BATTLE -> DRAW` closes the battle drawer/backdrop, leaves `Draw` enabled, and shows the full-width `Draw Phase` banner with no console errors.
- April 3, 2026 desktop arena stack pass: the `min-width: 980px` arena no longer fans enemy and player boards out side-by-side. `src/main/resources/static/css/style.css` now keeps the desktop battlefield in the same vertical enemy-over-player stack as mobile, stretches each half full-width, centers the board grids in one column, and enables vertical scrolling inside the arena when needed. Removed the now-redundant desktop portrait-only stack override and bumped `src/main/resources/static/index.html` to `style.css?v=34` so browsers pull the new layout immediately.
- Latest verification for the desktop arena stack pass: `.\mvnw.cmd -q -DskipTests compile` passes, the focused Playwright harness in `output/web-game/pc-vertical-focused-check` now loads `http://127.0.0.1:8080/css/style.css?v=34` and reports `battlefieldFlexDirection: "column"` with `overflowY: "auto"` and no console errors, and the screenshot in `output/web-game/pc-vertical-focused-check/pc-vertical-stacked.png` shows the live match arena stacked vertically on desktop with enemy above player in a single centered column.
- April 3, 2026 desktop split-shell rebuild: converted the wide desktop gameplay layout into a true two-column shell. `src/main/resources/static/index.html` now places the action bar, hand selector, preview panel, and deck preview inside a dedicated `desktop-menu` sidebar, while `src/main/resources/static/css/style.css` turns the `min-width: 980px` shell into a left arena / right menu grid. The arena now fills the left lane top-to-bottom with `overflow: hidden`, the menu column docks a large preview plus alphabetical deck counts, and the desktop loadout overlay now opens as a right-side panel instead of the old centered modal.
- Backend follow-up for the split shell: `src/main/java/com/sieglings/controller/GameController.java` now serializes the viewer-side `remainingDeck` card list alongside the hand so the right-side deck preview has live data instead of guessing from deck size alone.
- Frontend follow-up for the split shell: `src/main/resources/static/js/game.js` now renders the docked desktop preview panel from the focused card (falling back to the first hand card on wide screens), renders the docked deck-preview rows alphabetically with counts, and disables the old lifted-hand overlay in the new desktop-sidebar layout so the right column stays stable.
- Latest verification for the desktop split-shell rebuild: `.\mvnw.cmd -q -DskipTests compile` passes, `C:\Users\AlexTillman\AppData\Local\CodexTools\node-v24.14.1-win-x64\node.exe --check src/main/resources/static/js/game.js` passes, the required `develop-web-game` client ran against `http://127.0.0.1:8080` into `output/web-game/pc-split-client-check`, the right-docked loadout view was captured in `output/web-game/pc-split-layout-check/loadout-right-panel.png`, and a deterministic Playwright render using real local `/api/game/new -> /api/game/mulligan -> /api/game/draw` responses produced `output/web-game/pc-split-layout-check/match-split-shell.png` with `phaseBadge: "SETUP"`, `appShellColumns: "982.812px 403.188px"`, `battlefieldOverflowY: "hidden"`, `menuRightOfArena: true`, `previewHasCard: true`, `deckRowCount: 23`, `handCardCount: 6`, and no console errors.
